dashboard

<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Dashboard</title>
    @vite('resources/css/app.css')
</head>

<body class="bg-gray-400">

<div class="flex min-h-screen">

    <!-- SIDEBAR -->
    <aside class="w-64 bg-white shadow-lg hidden md:block">
        <div class="p-6 font-bold text-xl border-b">
            <img src="{{ asset('image/logoIBK.png') }}" alt="">
        </div>

        <nav class="p-4 space-y-2">
            <a href="/dashboard" class="block p-2 rounded bg-gray-200">Dashboard</a>
            <a href="/" class="block p-2 rounded hover:bg-gray-100">Home</a>
            <a href="/portofolio" class="block p-2 rounded hover:bg-gray-100">Portofolio</a>
        </nav>
    </aside>

    <!-- MAIN -->
    <div class="flex-1 flex flex-col">

        <!-- NAVBAR -->
        <header class="bg-white shadow p-4 flex justify-between items-center">
            <h1 class="text-xl font-semibold">Dashboard User</h1>

            <form method="POST" action="/logout">
                @csrf
                <button class="bg-black text-white px-4 py-2 rounded">
                    Logout
                </button>
            </form>
        </header>

       

            <!-- CONTENT -->
<main class="p-6 space-y-6">

    <!-- WELCOME -->
    <div class="bg-white p-6 rounded-xl shadow">
        <h2 class="text-2xl font-bold">
            Halo, {{ auth()->user()->name }}
        </h2>
        <p class="text-gray-500">
            Selamat datang di dashboard Indo Berkah Konstruksi
        </p>
    </div>

    <!-- REVIEW -->
    <div class="bg-white p-6 rounded-xl shadow">
        <h3 class="text-xl font-semibold mb-4">
            Berikan Review 
        </h3>

        <form action="{{ route('reviews.store') }}" method="POST">
            @csrf

            <div>
                <label class="block mb-2 text-sm font-medium text-gray-700">
                    Pengalaman Anda
                </label>

                <textarea
                    name="message"
                    rows="5"
                    class="w-full border border-gray-300 rounded-lg p-4 focus:ring-2 focus:ring-black focus:outline-none"
                    placeholder="Bagikan pengalaman Anda menggunakan jasa kami..."
                    required></textarea>
            </div>

            <button
                type="submit"
                class="mt-4 bg-black text-white px-5 py-2 rounded-lg hover:bg-gray-800 transition">
                Kirim Review
            </button>
        </form>

        @if(session('success'))
            <div class="mt-4 p-3 rounded-lg bg-green-100 text-green-700">
                {{ session('success') }}
            </div>
        @endif
    </div>

</main>
        
    </div>
</div>


</body>
</html>